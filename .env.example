# =============================================================================
# Localhost Tunnel - Root Environment Variables
# =============================================================================
# This is a monorepo with separate apps that have their own .env files.
#
# For app-specific configuration, see:
#   - apps/server/.env.example  - Web server and dashboard
#   - apps/cli/.env.example     - CLI client
#
# Quick Start:
#   1. Copy this file: cp .env.example .env
#   2. Copy app configs: cp apps/server/.env.example apps/server/.env
#   3. Update values as needed
#   4. Run: npm run dev
# =============================================================================

# Common variables that may be shared across apps
NODE_ENV="development"
