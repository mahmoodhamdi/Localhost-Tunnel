# =============================================================================
# Localhost Tunnel - CLI Environment Variables
# =============================================================================
# These environment variables can be set to configure the CLI behavior.
# You can also pass these as command-line arguments.
# =============================================================================

# -----------------------------------------------------------------------------
# Tunnel Password (Optional)
# -----------------------------------------------------------------------------
# Pre-set the tunnel password instead of using interactive prompt
# Useful for scripts and CI/CD environments
# LT_PASSWORD="your-tunnel-password"

# -----------------------------------------------------------------------------
# Server Configuration (Optional - usually set via CLI args)
# -----------------------------------------------------------------------------
# Default server URL if not specified via --host flag
# LT_HOST="https://tunnels.example.com"

# Default port to tunnel if not specified via --port flag
# LT_PORT="3000"

# Default subdomain if not specified via --subdomain flag
# LT_SUBDOMAIN="my-app"
